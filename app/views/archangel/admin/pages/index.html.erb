<div class="row wrapper border-bottom page-heading">
  <div class="col-lg-12">
    <h2>Page List</h2>

    <%= render "buttons" %>
  </div>
</div>

<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-lg-12">
      <% if @pages.any? %>
        <%= paginate @pages %>

        <table class="table table-striped table-hover">
          <thead>
            <tr>
              <th>Title</th>
              <th>Permalink</th>
              <th>Author</th>
              <th class="text-right">Action</th>
            </tr>
          </thead>

          <tbody>
            <% @pages.each do |page| %>
              <tr>
                <td><%= page.title %></td>
                <td><%= page.path %></td>
                <td><%= author_link(page.author) %></td>
                <td>
                  <ul class="nav nav-pills navbar-right">
                    <li><%= link_to "Show", admin_page_path(page), class: "btn btn-default btn-circle" %></li>
                    <li><%= link_to "Edit", edit_admin_page_path(page), class: "btn btn-primary btn-circle" %></li>
                    <li><%= link_to "Destroy", admin_page_path(page), class: "btn btn-danger btn-circle", method: :delete, data: { confirm: Archangel.t(:are_you_sure) } %></li>
                  </ul>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <%= paginate @pages %>
      <% else %>
        <p>
          <%= Archangel.t(:no_resources, resource: @pages.model_name.human.pluralize) %>
        </p>
      <% end %>
    </div>
  </div>
</div>
