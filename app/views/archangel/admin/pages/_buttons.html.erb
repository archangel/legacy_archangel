<ol class="buttons">
  <% if member_action? %>
    <li><%= link_to("Back", admin_pages_path) %></li>
  <% end %>

  <% if collection_action? || show_action? || edit_action? %>
    <li><%= link_to("New", new_admin_page_path) %></li>
  <% end %>

  <% if show_action? %>
    <li><%= link_to("Edit", edit_admin_page_path(@page)) %></li>
  <% end %>
</ol>
