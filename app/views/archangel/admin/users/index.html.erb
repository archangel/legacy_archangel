<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-12">
    <h2>User List</h2>

    <%= render "buttons" %>
  </div>
</div>

<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-lg-12 ibox-content">
      <% if @users.any? %>
        <%= paginate @users %>

        <div class="table-responsive">
          <table class="table table-striped table-hover">
            <thead>
              <tr>
                <th>Avatar</th>
                <th>Name</th>
                <th>Username</th>
                <th class="actions text-right">Actions</th>
              </tr>
            </thead>

            <tbody>
              <% @users.each do |user| %>
                <tr>
                  <td><%= image_tag(user.avatar.micro.url, class: "img-circle") %></td>
                  <td><%= user.name %></td>
                  <td><%= user.username %></td>
                  <td class="actions pull-right">
                    <%= link_to(fa_icon("question", text: "Show"), admin_user_path(user), class: "btn btn-info") %>
                    <%= link_to(fa_icon("check", text: "Edit"), edit_admin_user_path(user), class: "btn btn-success") %>
                    <%= link_to(fa_icon("times", text: "Destroy"), admin_user_path(user), class: "btn btn-danger", method: :delete, data: { confirm: Archangel.t(:are_you_sure) }) %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>

        <%= paginate @users %>
      <% else %>
        <p>
          <%= Archangel.t(:no_resources, resource: @users.model_name.human.pluralize) %>
        </p>
      <% end %>
    </div>
  </div>
</div>
