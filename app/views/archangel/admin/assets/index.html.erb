<div class="page-header panel panel-default">
  <div class="panel-body">
    <h2><%= Archangel.t(:index_resource, resource: @assets.model_name.human.titleize) %></h2>

    <%= render "buttons" %>
  </div>
</div>

<div class="assets-index panel panel-default">
  <div class="panel-body">
    <% if @assets.size %>

      <%= paginate @assets %>

      <div class="table-responsive">
        <table class="table table-hover table-bordered table-striped">
          <thead>
            <tr>
              <th scope="col"><%= Archangel.t(:file) %></th>
              <th scope="col"><%= Archangel.t(:title) %></th>
              <th class="actions text-right" scope="col"><%= Archangel.t(:actions) %></th>
            </tr>
          </thead>

          <tbody>
            <% @assets.each do |asset| %>
              <tr>
                <td><%= image_tag(asset.file.mini.url) %></td>
                <td><%= asset.title %></td>
                <td class="actions text-right">
                  <%= link_to(glyphicon_icon("info-sign", text: Archangel.t(:show)), admin_asset_path(asset), class: "btn btn-info btn-sm") %>
                  <%= link_to(glyphicon_icon("ok-sign", text: Archangel.t(:edit)), edit_admin_asset_path(asset), class: "btn btn-warning btn-sm") %>
                  <%= link_to(glyphicon_icon("remove-sign", text: Archangel.t(:destroy)), admin_asset_path(asset), class: "btn btn-danger btn-sm",
                                                            method: :delete, data: { confirm: Archangel.t(:are_you_sure) }) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <%= paginate @assets %>

    <% else %>

      <p class="no-resources">
        <%= Archangel.t(:no_resources, resource: @assets.model_name.human.pluralize.downcase) %>
      </p>

    <% end %>
  </div>
</div>
