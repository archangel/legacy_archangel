<div class="row wrapper border-bottom page-heading">
  <div class="col-lg-12">
    <h2>Tag List</h2>

    <%= render "archangel/admin/tags/buttons" %>

    <%= render "archangel/admin/shared/breadcrumbs" %>
  </div>
</div>

<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-lg-12">
      <% if @tags.any? %>
        <table class="table table-striped table-hover">
          <thead>
            <tr>
              <th>Name</th>
              <th>Slug</th>
              <th class="text-right">Action</th>
            </tr>
          </thead>

          <tbody>
            <% @tags.each do |tag| %>
              <tr>
                <td><%= tag.name %></td>
                <td><%= tag.slug %></td>
                <td>
                  <ul class="nav nav-pills navbar-right">
                    <li><%= link_to "Show", admin_tag_path(tag) %></li>
                    <li><%= link_to "Edit", edit_admin_tag_path(tag) %></li>
                    <li><%= link_to "Destroy", admin_tag_path(tag), method: :delete, data: { confirm: Archangel.t(:are_you_sure) } %></li>
                  </ul>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <p>
          <%= Archangel.t(:no_resources, resource: "Tags") %>
        </p>
      <% end %>
    </div>
  </div>
</div>
