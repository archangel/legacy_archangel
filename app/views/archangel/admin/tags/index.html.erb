<div class="row wrapper border-bottom page-heading">
  <div class="col-lg-12">
    <h2>Tag List</h2>

    <%= render "buttons" %>
  </div>
</div>

<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-lg-12">
      <% if @tags.any? %>
        <%= paginate @tags %>

        <table class="table table-striped table-hover">
          <thead>
            <tr>
              <th>Name</th>
              <th>Slug</th>
              <th class="text-right">Action</th>
            </tr>
          </thead>

          <tbody>
            <% @tags.each do |tag| %>
              <tr>
                <td><%= tag.name %></td>
                <td><%= tag.slug %></td>
                <td>
                  <ul class="nav nav-pills navbar-right">
                    <li><%= link_to "Show", admin_tag_path(tag), class: "btn btn-default btn-circle" %></li>
                    <li><%= link_to "Edit", edit_admin_tag_path(tag), class: "btn btn-primary btn-circle" %></li>
                    <li><%= link_to "Destroy", admin_tag_path(tag), class: "btn btn-danger btn-circle", method: :delete, data: { confirm: Archangel.t(:are_you_sure) } %></li>
                  </ul>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <%= paginate @tags %>
      <% else %>
        <p>
          <%= Archangel.t(:no_resources, resource: @tags.model_name.human.pluralize) %>
        </p>
      <% end %>
    </div>
  </div>
</div>
